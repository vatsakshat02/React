import React from "react";
import ReactDOM from "react-dom/client";
const heading = React.createElement("h1", {id:'heading'}, "Hello World from React" ) //this is an object render will convert it to h1 tag
const root =  ReactDOM.createRoot(document.getElementById('root'))



//the {} in create element is for the attributes you can add attributes over here like id, etc

/*
now to create a nested list we have to proceed like this 
so our nested array look like this
<div>
<div>
<h1>i am h1 </h1>
</div>
</div>

to create something like this we have to code like
*/

const parent = React.createElement(
    "div",
{id:"parent"},
React.createElement("div",{id:'child'},
React.createElement("h1",{},"i am an h1 tag")))


//basically to have siblings like :
/*
now to create a nested list we have to proceed like this 
so our nested array look like this
<div>
<div>
<h1>i am h1 </h1>
<h2>i am h2 </h2>
</div>
</div>

to create something like this we have to create an array of the the third argument
*/
const parents = React.createElement(
    "div",
{id:"parent"},
React.createElement("div",{id:'child'},
[React.createElement("h1",{},"yoyo Honey Singh"),
React.createElement("h2",{},"i am an h2 tag")]))

root.render(parents)